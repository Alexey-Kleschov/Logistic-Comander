import React from 'react';
import { View, Text } from 'react-native';
interface Props {};

const ScanerDataModal: React.SFC<Props> = props => {
    return (
        <View>
            <Text>
                * Scaned data in modal *
            </Text>
        </View>
    )
};

export default ScanerDataModal;